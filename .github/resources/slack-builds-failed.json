{
  "text": "🚨 Oops, The pushing to GitHub Packages failed with ${{ steps.get_version.outputs.VERSION }} version",
  "attachments": [
    {
      "color": "#d90000",
      "fields": [
        {
          "title": "Status",
          "short": true,
          "value": "Failed"
        },
        {
          "title": "Version",
          "short": true,
          "value": "${{ steps.get_version.outputs.VERSION }}"
        }
      ]
    }
  ]
}
