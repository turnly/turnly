name: 🔋 CI - Test Suites

on:
  pull_request:
    paths:
      - apps/**
    branches:
      - develop
      - main
  workflow_dispatch:

  push:
    paths:
      - apps/**
      - .github/workflows/global-continuous-integration-tests.yml
    branches:
      - develop
      - main

jobs:
  unit_and_integration:
    name: 🧩 Unit & Integration
    uses: ./.github/workflows/global-unit-and-integration-tests.yml
    with:
      mongo-username: root
      mongo-password: turnly
      mongo-db: turnly_testing_db

  e2e_and_benchmark:
    name: 🧩 E2E & Benchmark
    uses: ./.github/workflows/global-e2e-and-benchmark-tests.yml
