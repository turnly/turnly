version: '3.9'

services:
  dorothee:
    extends:
      file: common.yml
      service: common
    container_name: dorothee
    image: dorothee:latest
    build:
      context: ${DOCKER_RELATIVE_APPS_DIR}/Dorothee
      dockerfile: ./docker/Dockerfile.dev
      args:
        HTTP_PORT: ${HTTP_PORT}
        RPC_PORT: ${RPC_PORT}
    volumes:
      - ${DOCKER_RELATIVE_APPS_DIR}/Dorothee:/app
    depends_on:
      - gateway
      - mongo
    labels:
      - traefik.enable=true

      - traefik.http.routers.dorothee_rpc.entrypoints=rpc
      - traefik.http.routers.dorothee_rpc.rule=PathPrefix(`/turnly.dorothee.v1`)
      - traefik.http.routers.dorothee_rpc.service=dorothee_rpc
      - traefik.http.services.dorothee_rpc.loadbalancer.server.port=${RPC_PORT}
      - traefik.http.services.dorothee_rpc.loadbalancer.server.scheme=h2c
