version: '3.9'

services:
  gateway:
    extends:
      file: common.yml
      service: common
    image: turnly/gateway:latest
    container_name: gateway
    build:
      context: ${DOCKER_RELATIVE_APPS_DIR}/gateway
      dockerfile: Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - ${TRAEFIK_UI_PORT}:8080
      - 80:80
      - 443:443
    networks:
      turnly:

  # gateway-forward-auth:
  #   image: thomseddon/traefik-forward-auth:2
  #   environment:
  #     - PROVIDERS_GOOGLE_CLIENT_ID=your-client-id
  #     - PROVIDERS_GOOGLE_CLIENT_SECRET=your-client-secret
  #     - SECRET=something-random
  #     - INSECURE_COOKIE=true # Example assumes no https, do not use in production
  #   labels:
  #     - "traefik.http.middlewares.traefik-forward-auth.forwardauth.address=http://gateway-forward-auth:4181"
  #     - "traefik.http.middlewares.traefik-forward-auth.forwardauth.authResponseHeaders=X-Forwarded-User"
  #     - "traefik.http.services.traefik-forward-auth.loadbalancer.server.port=4181"

networks:
  turnly:
