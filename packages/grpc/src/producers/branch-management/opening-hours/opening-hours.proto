syntax = "proto3";

package turnly.branch_management.v1.opening_hours;

import "opening-hours.requests.proto";
import "opening-hours.responses.proto";

service OpeningHours {
  rpc Save(SaveOpeningHoursRequest) returns (SaveOpeningHoursResponse) {};
  rpc ListLocationHours(ListLocationHoursRequest)
    returns (ListLocationHoursResponse) {};
}
